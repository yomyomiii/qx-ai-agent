# QX Platform — 기획 의도 설명서

---

## 1. 타깃 및 서비스 목표

### 타깃 사용자

양자 컴퓨팅을 **처음 접하거나 독학 중인 입문~중급 학습자**를 1차 타깃으로 설정했습니다.
구체적으로는 다음 세 유형입니다.

| 유형 | 설명 |
|------|------|
| 학부·대학원생 | 양자정보·물리·컴퓨터공학 전공, 이론 수업을 실습으로 연결하고 싶은 학습자 |
| 연구 입문자 | QPU 관련 논문·자료를 읽으며 알고리즘을 직접 구현해보고 싶은 연구자 |
| 교육 담당자 | 커리큘럼을 설계하고 학습 진도를 관리해야 하는 강사·기관 |

### 핵심 문제 인식

기존 양자 컴퓨팅 학습에는 세 가지 분절이 있습니다.

- **이론 ↔ 실습 분절**: 논문·자료는 Notion에, 코드 실습은 Colab에, 메모는 따로 정리
- **도구 ↔ 이해 분절**: Qiskit 코드를 작성해도 회로가 무엇을 의미하는지 설명해주는 맥락이 없음
- **기록 ↔ 흐름 분절**: 배운 내용이 대화창을 닫으면 사라지고, 지식이 축적되지 않음

### 서비스 목표

> **"자료 선택 → AI와 대화 → 회로 구현 → 지식 기록"이 하나의 화면 안에서 끊기지 않는 양자 컴퓨팅 학습 워크스페이스**

---

## 2. 컨셉

**QX Platform**은 AI를 학습 파트너로 삼아 양자 컴퓨팅을 배우는 **3-패널 통합 학습 환경**입니다.

```
[자료·커리큘럼]  ←→  [AI 채팅]  ←→  [회로 에디터 + 노트]
   Left Panel         Center           Right Panel
```

각 패널은 독립적이지 않고 **맥락을 공유**합니다.
- 왼쪽에서 선택한 자료가 AI 채팅의 컨텍스트로 자동 주입됩니다.
- AI가 생성한 회로는 오른쪽 에디터로 즉시 불러올 수 있습니다.
- 대화와 회로는 오른쪽 노트에 저장되며, 지식 그래프로 연결됩니다.

### 핵심 컨셉 키워드

- **AI-Native**: 모든 학습 흐름에 AI가 내재되어 있음 (별도 탭 전환 없음)
- **Context-Aware**: 내가 선택한 자료·회로에 맞춰 AI 응답과 빠른 액션이 달라짐
- **Circuit-First**: 텍스트 학습이 아닌 회로 구현이 학습의 중심
- **Knowledge Accumulation**: 대화·분석·직접 메모가 하나의 노트로 축적됨

---

## 3. 레퍼런스 서비스 분석

### 3-1. Google Colab — "브라우저에서 코드 실행"

| 참고한 것 | QX에서의 적용 |
|-----------|---------------|
| 브라우저만으로 코드를 실행할 수 있는 접근성 | 설치 없이 즉시 회로를 조작할 수 있는 환경 |
| 셀 단위 실행과 결과 시각화 | 회로 시뮬레이션 결과를 인라인으로 바로 확인 |
| Qiskit 코드뷰 | 비주얼 에디터와 Qiskit 코드뷰를 함께 제공 (탭 전환으로 동기화) |

**Colab과의 차이**: Colab은 Python 문법을 알아야 하고, AI 설명이 없으며, 메모가 따로 관리됩니다. QX는 코드 없이 시작할 수 있고, AI가 게이트 하나하나를 설명하며, 메모가 워크스페이스에 통합됩니다.

---

### 3-2. NotebookLM — "자료 기반 AI 질의응답"

| 참고한 것 | QX에서의 적용 |
|-----------|---------------|
| 업로드한 문서를 소스로 선택해 AI와 대화 | 자료·레퍼런스 회로를 선택하면 해당 맥락이 AI에 주입됨 |
| 여러 자료를 동시에 컨텍스트로 사용 | 문서 N개 + 레퍼런스 회로 M개를 동시에 선택 가능 |
| 소스 기반 요약·인용 | "자료 요약", "핵심 개념", "학습 질문" 빠른 액션 버튼 |

**NotebookLM과의 차이**: NotebookLM은 텍스트 문서만 소스로 사용하고, 회로라는 개념이 없습니다. QX는 **회로 자체가 1급 자료(first-class resource)**로, 회로를 소스로 선택해 "이 회로를 설명해줘", "개선안을 줘"와 같은 맥락 질의가 가능합니다.

---

### 3-3. Tiro — "AI 노트 생성·정리"

| 참고한 것 | QX에서의 적용 |
|-----------|---------------|
| 대화나 작업 결과를 자동으로 노트화 | AI 응답을 "메모 저장" 버튼 하나로 노트에 추가 |
| AI가 요약·정리 문서를 생성 | 대화 요약, 개념 마인드맵, 용어 정리 자동 생성 |

**Tiro와의 차이**: Tiro는 범용 글쓰기 도우미입니다. QX의 노트 생성은 **양자 컴퓨팅 도메인에 특화**되어 있으며, 회로 분석 보고서·회로 비교·실험 보고서처럼 **회로와 연결된 출력 타입**이 존재합니다. 총 6가지 자동 생성 타입(대화 요약, 마인드맵, 용어 정리, 회로 분석, 회로 비교, 실험 보고서)을 제공합니다.

---

### 3-4. Obsidian — "지식 그래프, 노트 연결"

| 참고한 것 | QX에서의 적용 |
|-----------|---------------|
| 노트 간 링크를 시각적 그래프로 표현 | 노트의 참조(basis) 기반으로 지식 그래프 자동 생성 |
| 허브-노드 구조의 지식 맵 | 회로·대화를 허브로, 메모·분석 노트를 노드로 배치 |

**Obsidian과의 차이**: Obsidian은 사용자가 수동으로 `[[링크]]`를 걸어야 그래프가 생성됩니다. QX는 노트를 저장할 때 **참조 출처(basis)가 자동으로 기록**되기 때문에 별도 작업 없이 그래프가 만들어집니다. AI가 생성한 요약은 "전체 대화"를, 회로 분석 노트는 해당 회로명을 자동으로 basis로 갖습니다.

---

## 4. 독립적인 아이데이션 (레퍼런스 서비스에 없는 것)

4개 레퍼런스 어디에도 없는, QX 고유의 기획 요소입니다.

### 4-1. 회로 출처 추적 (Circuit Provenance)
회로가 어떻게 만들어졌는지를 태그로 추적합니다.
- `AI 응답` — AI가 채팅에서 생성한 회로
- `직접 작성` — 사용자가 에디터에서 만든 회로
- `샘플` — 플랫폼이 제공하는 레퍼런스 회로

회로 목록과 에디터 헤더 양쪽에 태그가 일관되게 표시됩니다. "내가 이 회로를 AI한테 받은 건지, 내가 짠 건지"를 즉시 파악할 수 있습니다.

### 4-2. 맥락 연동 빠른 액션 버튼
선택한 자료 조합에 따라 채팅 하단 버튼이 동적으로 변합니다.

| 선택 상태 | 표시되는 액션 |
|-----------|---------------|
| 문서 1개 | 자료 요약, 핵심 개념, 학습 질문, 회로 생성 |
| 문서 N개 | 자료 비교, 통합 요약, 학습 로드맵, 회로 생성 |
| 회로 1개 | 회로 설명, 개선 제안, Qiskit 변환, 유사 알고리즘 |
| 문서 + 회로 혼합 | 이론-회로 연결, 구현 검증, 개념 보완, 통합 학습 가이드 |

"어떻게 물어봐야 할지 모르겠다"는 학습자의 진입 장벽을 낮추기 위한 장치입니다.

### 4-3. AI 자료 탐색 모드
"어떤 자료를 보면 되는지도 모르겠다"는 상황을 위한 모드입니다. AI가 역질문을 통해 학습자의 수준과 관심사를 파악한 뒤, 맞춤 자료·회로를 추천하고 그 자리에서 자료 목록에 추가할 수 있습니다.

### 4-4. 회로 → AI 디버깅 파이프라인
에디터의 디버깅 패널에서 버튼 하나로 현재 회로 구성(게이트 배치·깊이)을 AI에게 전달해 오류 진단이나 개선 제안을 받을 수 있습니다. 회로와 AI 채팅이 단방향이 아니라 **쌍방향으로 연결**되어 있습니다.

### 4-5. 비주얼 에디터 ↔ Qiskit 코드 동기화
게이트를 클릭해서 배치하는 시각적 에디터와 `from qiskit import QuantumCircuit` 형태의 코드 뷰가 탭 전환 시 자동 동기화됩니다. 코딩을 모르는 학습자가 시각적으로 회로를 만들면서 자연스럽게 코드 구조도 익힐 수 있도록 설계했습니다.

### 4-6. 회로 연동 출력 생성 (6종)
노트 생성이 텍스트 대화에만 머물지 않습니다. 저장된 회로를 직접 선택해서 **회로 분석 보고서**, **회로 비교 분석**, **실험 보고서**를 생성할 수 있습니다. 학습 기록이 실험 포트폴리오로 발전할 수 있습니다.

### 4-7. 협업 탭 (Collab)
개인 학습 워크스페이스지만, 공유 설정된 노트가 팀 공간에 모이는 협업 레이어가 존재합니다. 그룹 스터디나 강의 환경에서 학습자 간 지식 공유가 가능합니다.

---

## 5. 경쟁 서비스 대비 장점

| 비교 항목 | Colab | NotebookLM | Tiro | Obsidian | **QX Platform** |
|-----------|-------|------------|------|----------|-----------------|
| 진입 장벽 | Python 필요 | 낮음 | 낮음 | 중간 | **코드 없이 시작 가능** |
| 도메인 특화 | 범용 | 범용 | 범용 | 범용 | **양자 컴퓨팅 전용** |
| 회로 조작 | ✓ (코드) | ✗ | ✗ | ✗ | **✓ (비주얼 + 코드)** |
| AI 맥락 주입 | ✗ | 문서만 | ✗ | ✗ | **문서 + 회로 동시** |
| 지식 그래프 | ✗ | ✗ | ✗ | ✓ (수동) | **✓ (자동)** |
| 출력 생성 유형 | ✗ | 요약 정도 | 글쓰기 | ✗ | **6종 (회로 포함)** |
| 회로 출처 추적 | ✗ | ✗ | ✗ | ✗ | **✓** |
| 퀴즈·연습문제 | ✗ | ✗ | ✗ | ✗ | **✓ (커리큘럼 연동)** |

**핵심 요약**: QX는 4개 레퍼런스 서비스가 각자 잘하는 것을 **양자 컴퓨팅이라는 하나의 맥락 안에서 통합**합니다. 각 툴을 따로 열 필요 없이, 자료→대화→회로→기록 루프가 하나의 워크스페이스 안에서 완결됩니다.

---

## 6. 기능적 개선 방향 및 후속 계획

개발 구현이 아닌 **서비스 기획·경험 측면**의 발전 방향입니다.

### 단기 (프로토타입 고도화)

- **자료 직접 업로드**: PDF·링크를 드래그앤드롭으로 추가해 즉시 소스로 활용
- **회로 공유·퍼블리싱**: 만든 회로를 링크로 공유하거나 커뮤니티에 등록
- **학습 진도 대시보드**: 커리큘럼별 완료율, 퀴즈 점수 추이, 회로 누적 수 시각화
- **오답 복습 루프**: 퀴즈에서 틀린 문제를 다음 세션에 자동으로 다시 제시

### 중기 (사용 맥락 확장)

- **강의실 모드**: 강사가 커리큘럼을 설계하고 학생의 제출 회로·퀴즈 결과를 모아보는 관리 화면
- **스터디 그룹 공간**: 실시간으로 같은 회로를 보며 AI와 토론하는 협업 세션
- **회로 버전 관리**: 에디터에서 수정 이력을 저장하고 이전 버전으로 되돌리는 기능

### 장기 (실 QPU 연동)

- **실제 양자 하드웨어 실행**: IBM Quantum, IonQ 등 실제 QPU에 회로를 제출하고 결과를 플랫폼 안에서 확인
- **노이즈 분석**: 실 QPU 결과와 시뮬레이션 결과의 차이를 AI가 설명
- **알고리즘 포트폴리오**: 학습 과정에서 만든 회로들을 포트폴리오 형식으로 정리·공개

---

*QX Platform v5 — 프로토타입 기획의도서*
